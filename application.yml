spring:
  application:
    name: CONFIG-DEFAULT

eureka:
  instance:
    prefer-ip-adddress: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
